# Port the Express server listens on
PORT=5001

# Origin allowed to make CORS requests (your React dev server)
ALLOWED_ORIGIN=http://localhost:5173

# Path to the JSON file used to persist contact form submissions
DATA_FILE=./data/contacts.json
